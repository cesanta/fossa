machine:
  pre:
    - sudo pip install cpp-coveralls

checkout:
  post:
    - git clone https://github.com/cesanta/frozen.git /home/ubuntu/frozen

test:
  pre:
    - make -C test ismerged
    - make -C modules
    - make -C test
    - make -C examples
